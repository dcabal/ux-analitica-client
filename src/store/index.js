import { createStore } from 'vuex';
import OwnerModule from './modules/owner-module';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
      OwnerModule
  }
});